<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo Application</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="/style/style.css" />
</head>

<body>

    <div id="todo-table" class="container mt-5">
        <div class="row">
            <div class="col-md-4 mt-2 order-1 order-md-2">
                <form method="post">

                    <div class="input-group mb-3">
                        <label for="item"></label>
                        <input type="text" class="form-control" name="item" id="item" placeholder="Add new item">
                        <div class="input-group-append">
                            <button class="btn btn-def btn-block" type="submit">&#43;&nbsp;Add</button>
                        </div>
                    </div>

                </form>
            </div>
            <div class="col-md-8 order-2 order-md-1">
                <div class="card">
                    <div class="card-body shadow-sm">
                        <% if (todos.length> 0) {%>

                            <ul class="list-group list-group-flush text-dark font-weight-bold">

                                <% for (let i=0; i < todos.length; i++){ %>
                                    <li class="list-group-item w-100" title="click to delete task">
                                        <%= todos[i].item %>
                                    </li>
                                    <% } %>
                            </ul>
                            <%} else { %>
                                <p class="text-white mb-0">All task have been <strong>completed!</strong></p>
                                <% } %>
                    </div>
                </div>
            </div>
        </div>

    </div>
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
    <script type="text/javascript" src="asset/todo.js"></script>
</body>

</html>